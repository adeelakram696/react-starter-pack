import { combineReducers } from 'redux';
// import breadcrumbsReducer from '../../shared/breadcrumbs/breadcrumbsReducer';

const rootReducer = combineReducers({
  // breadcrumbs: breadcrumbsReducer,
});

export default rootReducer;
